# Copy to .env.local for local dev, or set this in the Vercel dashboard
# (Project Settings -> Environment Variables) for production.
# Must be the HTTPS URL of the Render backend (no trailing slash).
VITE_API_BASE=https://crypto-leads-api.onrender.com
